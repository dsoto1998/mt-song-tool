# macOS resource forks
._*
.DS_Store

# Claude Code session data
.claude/

# Build output / release zips
Versions/

# Runtime logs
logs/

# FFmpeg binaries (large — bundled separately at build time)
AudioConverter/

# Python build artifacts
mtst-master/build/
mtst-master/dist/
mtst-master/__pycache__/
mtst-master/**/__pycache__/
**/*.pyc

# Python venv (rebuilt by build_parser.sh)
mtst-master/venv/

# Swift build artifacts
mtst-master/swift-app/.build/

# PyInstaller _internal libs (large ML deps — rebuilt by build_parser.sh)
mtst-master/swift-app/dist/parse_als/_internal/

# Test files
test-ableton-files/
